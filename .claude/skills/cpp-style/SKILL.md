---
name: cpp-style
description: Правила C++ для voxelworld — trailing return types, типы проекта, политика комментариев, устройство модульного кода (.cppm/.cpp, партиции, import std, прелюдии, логирование) и рецепты добавления компонентов, систем, инструментов и операций. Читай перед любой правкой C++ в engine/, apps/ или tests/.
---

# C++ в voxelworld

## Стиль

**Trailing return type — всегда:**
```cpp
[[nodiscard]] auto get_value() const -> int32;
auto operator=(const foo&) -> foo& = delete;
```

**Типы проекта вместо встроенных и std:** `uint8..uint64`, `int8..int64`,
`float32`, `float64` (из `vw.core`). `int`/`float` в новом коде не пишем.

**Именование:** типы, функции, методы, поля, константы — `snake_case`; параметры
шаблонов — `PascalCase` (`T`, `Cs`); пространства имён — `vw::`, `vw::spatial::`,
`vw::asset::`, `vw::ecs::`, `vw::plat::`, `vw::gfx::`, `vw::sculptor::`.

**Приватные поля — с подчёркиванием в конце** (`world_`, `entity_`), приватные
методы — тоже (`update_entity_`), если это деталь реализации класса.

**Комментарии:**
- Только у классов и структур верхнего уровня — короткое «зачем оно».
- NEVER в реализациях (`.cpp`).
- NEVER очевидное (`// Calculate index` перед вычислением индекса).
- NEVER разделители секций (`// ===== Section =====`).
- Исключение — неочевидная логика, где важен замысел, а не механика.
- Комментарии в коде пишем по-английски.

**Инициализация:** конструктор вместо `init()`/`setup()`. Зависимости приходят
параметрами конструктора, а не сеттерами.

**Ошибки:** исключение — фатальный путь (нарушение инварианта), ловится только на
границе `main()`. Обрабатываемые ошибки — `std::expected`. В горячих путях
исключений нет.

**Заголовки (пока живо `engine/include/`):** `#pragma once` плюс ifndef-страж;
порядок включений — стандартная библиотека, сторонние, `vw/...`, локальные.

## Модульный код (`engine/core/`, `engine/ecs/`, `engine/world/`, `engine/platform/`)

Раскладка: компонент = каталог со своим `CMakeLists.txt`, исходники в `<lib>/src/`,
интерфейсные `.cppm` и имплементационные `.cpp` лежат рядом. Деления `include/` vs
`src/` нет — публичность выражается `export`.

- Партиция = один `.cppm`. Взаимно зависимые сущности обязаны лежать в одной
  партиции: циклы между партициями запрещены (поэтому `math` и `transform`
  вместе, и вся геометрия `aabb`/`ray`/`plane`/`frustum` тоже).
- Внутри одного модуля форвард-объявление между партициями законно (`class world;`
  в `:systems`, определение — в первичном юните). Снаружи модуля объявлять его
  сущности нельзя — только импортировать.
- Интерфейс в `.cppm`, реализация нешаблонного кода в `.cpp`. `.inl.h` в модульном
  коде не бывает.
- В интерфейсной партиции остаются: шаблоны, `constexpr` и тела, которые зовут
  пер-воксельно или пер-узлово (`model::get_voxel`, аксессоры компонентов,
  `aabb::intersects`). Через границу модуля инлайнится только то, чьё определение
  лежит в интерфейсе — это измерено бенчмарком в M2.
- В `.cpp` не пиши `inline`: определение обязано быть единственным.
- Стандартная библиотека: в `.cppm` — через global module fragment (`module;` +
  `#include`), и тот же набор заголовков дублируется в
  `vw/<lib>/detail/module_prelude.h`; в `.cpp` — `import std;`.
- NEVER `import std` в **экспортируемой** партиции до M6: потребители пока
  включают стандартные заголовки текстуально, и это ломает их сборку. В
  имплементационных юнитах и во внутренних партициях — обязателен.
- Логирование внутри модуля идёт через имплементационную партицию (образцы —
  `engine/world/src/logging.cppm`, `engine/platform/src/logging.cppm`):
  `vw/log/logger.h` в модульный юнит не включить, внутри него `import vw.core`,
  а импорт вне преамбулы запрещён.
- Колбэк стороннего C-API с точной сигнатурой держи в имплементационном юните:
  интерфейс объявляет `friend struct detail::<name>_callbacks`, определение
  живёт в `.cpp` (образец — окно и GLFW).
- Настройки сборки доходят до кода экспортированным `constexpr`, а не макросом
  (образец — `vw::log::min_level`). Макрос в заголовке потребителя ломает ODR,
  если TU соберут его по-разному.

Заголовки в `engine/include/vw/{core,ecs,asset,spatial,platform}/` — шимы: прелюдия, затем
импорты модулей. Новый код в них не добавляем, они удаляются в M6.

## Рецепты

**Новый компонент ECS:** структура в `engine/world/src/components.cppm`, namespace
`vw::ecs`, приватные поля + аксессоры, дружба с системой, которая их меняет. Тела
аксессоров — прямо в классе. Отдельная регистрация не нужна: идентификатор
выдаётся лениво по первому обращению.

**Новая система ECS:** объявление в `engine/world/src/systems.cppm`, тело — новый
`engine/world/src/<name>_system.cpp` (`module vw.world;` + `import std;`), файл
добавить в `engine/world/CMakeLists.txt`. Систему включить в кортеж
`world::systems` в `world.cppm`. Необязательные хуки `on_add<C>`/`on_remove<C>`/
`shutdown()` подхватываются концептами автоматически.

**Пер-кадровый обход:** `registry::for_each<Cs...>(fn)`, а не `view` — заметно
быстрее, разбор в `docs/m2-vw-ecs.md`.

**Новый инструмент Sculptor:** класс-наследник `base_tool` в
`apps/sculptor/src/tools/`, реализовать виртуальные методы, зарегистрировать в
панели инструментов.

**Новая операция Sculptor (undo/redo):** класс-наследник `base_operation` в
`apps/sculptor/src/operations/`, реализовать `execute()` и `undo()`, вызывать
через `operation_manager::execute()`.

## После правок

Изменил что-то в `engine/` — прогони тесты (см. навык `cmake-build`).
