export module vw.world:terrain;

// Собирает генерацию рельефа: интерфейс генератора, колонка в работе, загрузчик
// и генератор на шуме Перлина.
export import :terrain.generator;
export import :terrain.column;
export import :terrain.loader;
export import :terrain.perlin;
